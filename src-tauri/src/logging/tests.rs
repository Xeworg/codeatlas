//! Unit tests for the dev-mode per-execution file logging module.
//!
//! Each test file under `tests/` covers one slice of the public helper API.
//! Test modules are kept small and focused so failures point at specific gaps.

mod dev_logging_tests;
